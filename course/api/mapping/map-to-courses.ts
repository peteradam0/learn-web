import { Course } from "@/course/domain/course"

export const mapToCourses = (apiData: any): Course[] => {
  return apiData
}

export const mapToCourse = (apiData: any): Course => {
  return apiData
}
